# GitHub repo metadata

## Description (repo tagline)
Profile, optimize, and safely shrink the LLM cost & latency of knowledge-graph ingestion pipelines. Graphiti-first. Cut cost — proven safe by an eval gate.

## Topics
`llm` · `inference-optimization` · `cost-optimization` · `latency` · `graphiti` · `knowledge-graph` · `agent-memory` · `rag` · `llm-evaluation` · `llm-observability` · `fastapi` · `react` · `typescript` · `python` · `open-source` · `developer-tools` · `prompt-compression` · `semantic-cache`

## Social preview text
**GraphThrift** — the cost/latency optimizer for agent-memory ingestion. Batches the per-edge LLM fan-out, caches restated facts, compresses prompts, and blocks any optimization that drops extraction F1. One line to adopt on Graphiti.

## Pinned release note (v0.1.0)
See CHANGELOG.md. Headline: one-line Graphiti `LLMClient` wrap; safe config cuts ~47% cost / 40% calls with F1 unchanged (gate PASS); an aggressive config is auto-flagged unsafe. FastAPI + React dashboard, Docker one-command startup, CI green.
